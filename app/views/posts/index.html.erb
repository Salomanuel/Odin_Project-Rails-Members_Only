<h1>all posts</h1>

<ul>
	<% @posts.each do |post| %>
		<li>
			<% user = User.find(post.user_id) %>
			<%= link_to post.title, post %>
			<% if !@current_user.nil? %>
				by <%= link_to user.name, user %>	
			<% end %>
		</li>
	<% end %>
</ul>